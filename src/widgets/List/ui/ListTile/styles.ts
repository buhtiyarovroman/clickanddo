import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    paddingTop: 10,
    width: '100%',
    paddingHorizontal: 20,
  },
  column: {
    flex: 1,
    paddingHorizontal: 5,
  },
})
