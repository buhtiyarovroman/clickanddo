import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  scroll: {
    alignItems: 'center',
    width: '100%',
  },
  interestsWrapper: {
    paddingHorizontal: 20,
    width: '100%',
    marginTop: 20,
  },
})
