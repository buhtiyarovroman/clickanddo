import { FlexWrapper } from '@/shared/ui/Styled/Styled'
import styled from 'styled-components'

export const Container = styled(FlexWrapper).attrs({ flexDirection: 'column' })`
  padding: 12px 16px;
`
