import { IMAGE_HOST } from '@env'

const imgUrl = `${IMAGE_HOST}`

export const images = {
  user: `${imgUrl}/user/`,
  publication: `${imgUrl}/publication/`,
  interestCategory: `${imgUrl}/interest-category/`,
  project: `${imgUrl}/project/`,
  chat: `${imgUrl}/chat/`,
}
