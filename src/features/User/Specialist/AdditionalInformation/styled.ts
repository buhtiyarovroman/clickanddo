import styled from 'styled-components'
import { EColors } from '@/shared/ui/Styled'
import { Hr } from '@/shared/ui/Styled/Styled'

export const Line = styled(Hr).attrs({
  mTop: '20px',
  mBottom: '20px',
  color: EColors.grey_300,
})``
