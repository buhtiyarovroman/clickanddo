import { EColors } from '@/shared/ui/Styled'
import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  main: {
    backgroundColor: EColors.white,
  },
  spec: { paddingHorizontal: 20, paddingTop: 20 },
})
